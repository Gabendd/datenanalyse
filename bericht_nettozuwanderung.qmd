---
title: "Der Effekt des BIP-Wachstums auf die Nettozuwanderung in die Schweiz"
author: "Vincent Pickert - 2020-061-610"
author-title: "Autor"
date: today
format:
  html:
    toc: true
    toc-depth: 3
    toc-title: "Inhaltsverzeichnis"
    theme: cosmo
    self-contained: true
  pdf:
    toc: true
    toc-title: "Inhaltsverzeichnis"
    number-sections: true
    geometry: margin=2.5cm
    keep-tex: false
lang: de
execute:
  echo: false
  warning: false
  message: false
---

```{r}
#| label: setup

erforderliche_pakete <- c(
  "readr", "dplyr", "stringr", "stringi", "ggplot2", "WDI",
  "rnaturalearth", "countrycode", "huxtable", "scales", "sf",
  "tibble", "broom", "stargazer", "texreg", "tidyr", "codetools",
  "rvest", "gtsummary", "gt", "kableExtra"
)

options(scipen = 999)
options(repos = c(CRAN = "https://cloud.r-project.org"))

fehlende_pakete <- setdiff(erforderliche_pakete, rownames(installed.packages()))
if (length(fehlende_pakete) > 0) install.packages(fehlende_pakete)

invisible(lapply(erforderliche_pakete, library, character.only = TRUE))

pfade <- c("tabellen", "grafiken", "data")
invisible(lapply(pfade, function(p) {
  if (!dir.exists(p)) dir.create(p, recursive = TRUE)
}))
```

```{r}
#| label: daten-import

if (file.exists("data/swiss_immigration_countries_year.csv")) {
  rohe_schweizer_einwanderung <- readr::read_csv(
    "data/swiss_immigration_countries_year.csv", show_col_types = FALSE
  )
} else {
  rohe_schweizer_einwanderung <- readr::read_csv(
    "https://raw.githubusercontent.com/Gabendd/datenanalyse/main/data/swiss_immigration_countries_year.csv",
    show_col_types = FALSE
  )
}

rohe_schweizer_einwanderung <- rohe_schweizer_einwanderung |>
  dplyr::mutate(
    jahr              = as.integer(year),
    netto_zuwanderung = as.numeric(net_migration)
  ) |>
  dplyr::rename(herkunft = origin_en) |>
  dplyr::filter(
    !herkunft %in% c(
      "Afrique", "Amérique", "Asie", "Océanie",
      "EFTA countries", "Yugoslavia", "Serbia and Montenegro"
    ),
    !stringr::str_detect(herkunft, "Renseignements|Source|© OFS")
  ) |>
  dplyr::select(herkunft, jahr, netto_zuwanderung) |>
  dplyr::arrange(herkunft, jahr)

jahresdaten_schweizer_einwanderung <- rohe_schweizer_einwanderung |>
  dplyr::group_by(jahr) |>
  dplyr::summarize(
    netto_zuwanderung = sum(netto_zuwanderung, na.rm = TRUE),
    .groups = "drop"
  ) |>
  dplyr::arrange(jahr)
```

```{r}
#| label: worldbank-import

indikatoren_liste <- tibble::tibble(
  Code = c("NY.GDP.MKTP.KD.ZG", "SL.UEM.TOTL.ZS"),
  Description = c(
    "BIP-Wachstum (jährlich in %)",
    "Arbeitslosigkeit, insgesamt (% der Erwerbsbevölkerung)"
  )
)

indikatoren         <- indikatoren_liste$Code
weltbank_datei_pfad <- "data/world_bank_raw.rds"
start_jahr <- min(jahresdaten_schweizer_einwanderung$jahr, na.rm = TRUE)
end_jahr   <- max(jahresdaten_schweizer_einwanderung$jahr, na.rm = TRUE)

weltbank_daten_abrufen <- function() {
  WDI::WDI(
    country   = "CHE",
    indicator = indikatoren,
    start     = start_jahr,
    end       = end_jahr,
    extra     = TRUE
  )
}

if (!file.exists(weltbank_datei_pfad)) {
  weltbank_rohe_daten <- weltbank_daten_abrufen()
  saveRDS(weltbank_rohe_daten, weltbank_datei_pfad)
} else {
  temp_pruefung    <- readRDS(weltbank_datei_pfad)
  fehlend_in_datei <- setdiff(indikatoren, names(temp_pruefung))
  if (length(fehlend_in_datei) > 0) {
    weltbank_rohe_daten <- weltbank_daten_abrufen()
    saveRDS(weltbank_rohe_daten, weltbank_datei_pfad)
  } else {
    weltbank_rohe_daten <- temp_pruefung
  }
  rm(temp_pruefung, fehlend_in_datei)
}

schweizer_weltbank_daten <- weltbank_rohe_daten |>
  dplyr::select(year, dplyr::all_of(indikatoren)) |>
  dplyr::rename(
    jahr              = year,
    bip_wachstum      = NY.GDP.MKTP.KD.ZG,
    arbeitslosenquote = SL.UEM.TOTL.ZS
  ) |>
  dplyr::arrange(jahr)

analysedaten_schweiz <- jahresdaten_schweizer_einwanderung |>
  dplyr::left_join(schweizer_weltbank_daten, by = "jahr") |>
  dplyr::arrange(jahr)
```

```{r}
#| label: visualisierungen

welt <- rnaturalearth::ne_countries(scale = "medium", returnclass = "sf")

karten_daten <- rohe_schweizer_einwanderung |>
  dplyr::group_by(herkunft) |>
  dplyr::summarise(
    netto_zuwanderung = sum(netto_zuwanderung, na.rm = TRUE),
    .groups = "drop"
  ) |>
  dplyr::mutate(
    iso_a3 = ifelse(
      herkunft == "Kosovo", "XKX",
      countrycode::countrycode(herkunft, "country.name", "iso3c")
    )
  ) |>
  dplyr::filter(!is.na(iso_a3)) |>
  dplyr::mutate(
    herkunft_de = ifelse(
      herkunft == "Kosovo", "Kosovo",
      countrycode::countrycode(
        herkunft, origin = "country.name.en", destination = "country.name.de"
      )
    )
  )

karten_laender_liste <- sort(karten_daten$herkunft_de)

weltkarte_sf <- welt |>
  dplyr::mutate(iso_a3 = dplyr::coalesce(iso_a3_eh, iso_a3)) |>
  dplyr::filter(
    continent == "Europe", !name_long %in% "Russian Federation",
    !is.na(iso_a3), iso_a3 != "-99"
  ) |>
  dplyr::left_join(karten_daten, by = "iso_a3")

schweiz_migrations_weltkarte <- ggplot2::ggplot(weltkarte_sf) +
  ggplot2::geom_sf(ggplot2::aes(fill = netto_zuwanderung),
                   color = "white", linewidth = 0.2) +
  ggplot2::scale_fill_gradient(low = "#deebf7", high = "#08519c",
                                na.value = "grey90") +
  ggplot2::labs(
    title = "Nettozuwanderung in die Schweiz nach Herkunftsland (Europa)",
    fill  = ""
  ) +
  ggplot2::theme_void() +
  ggplot2::theme(
    plot.title          = ggplot2::element_text(face = "plain", size = 21, hjust = 0.5),
    plot.title.position = "plot",
    legend.title        = ggplot2::element_blank(),
    legend.text         = ggplot2::element_text(size = 21)
  )

kurven_diagramm <- ggplot2::ggplot(
  jahresdaten_schweizer_einwanderung,
  ggplot2::aes(x = jahr, y = netto_zuwanderung)
) +
  ggplot2::geom_line(color = "#08519c", linewidth = 1) +
  ggplot2::geom_point(color = "#08519c", size = 2) +
  ggplot2::labs(x = "Jahr", y = "Nettozuwanderung",
                title = "Nettozuwanderung in die Schweiz nach Jahr") +
  ggplot2::theme_minimal() +
  ggplot2::scale_x_continuous(breaks = seq(1990, 2025, by = 5)) +
  ggplot2::scale_y_continuous(
    labels = scales::comma_format(big.mark = ".", decimal.mark = ",")) +
  ggplot2::theme(
    plot.title  = ggplot2::element_text(face = "plain", size = 21, hjust = 0.5),
    axis.title  = ggplot2::element_text(size = 22),
    axis.text   = ggplot2::element_text(size = 22),
    axis.text.x = ggplot2::element_text(size = 14, margin = ggplot2::margin(t = 10))
  )

jahresdaten_mit_vorzeichen <- jahresdaten_schweizer_einwanderung |>
  dplyr::mutate(
    migration_sign = ifelse(netto_zuwanderung >= 0,
                            "Einwanderung (positiv)", "Auswanderung (negativ)")
  )

boxplot_vorzeichen <- ggplot2::ggplot(
  jahresdaten_mit_vorzeichen,
  ggplot2::aes(x = migration_sign, y = netto_zuwanderung, fill = migration_sign)
) +
  ggplot2::geom_boxplot() +
  ggplot2::scale_fill_manual(
    values = c("Einwanderung (positiv)" = "#2ca25f",
               "Auswanderung (negativ)" = "#de2d26"),
    name = "Migrationsrichtung"
  ) +
  ggplot2::labs(x = "", y = "Nettozuwanderung",
                title = "Nettozuwanderung nach Vorzeichen") +
  ggplot2::theme_minimal() +
  ggplot2::scale_y_continuous(
    labels = scales::comma_format(big.mark = ".", decimal.mark = ",")) +
  ggplot2::theme(
    plot.title          = ggplot2::element_text(face = "plain", size = 21, hjust = 0.5),
    plot.title.position = "plot",
    axis.title          = ggplot2::element_text(size = 22),
    axis.text           = ggplot2::element_text(size = 22),
    axis.text.x         = ggplot2::element_blank(),
    axis.ticks.x        = ggplot2::element_blank(),
    legend.title        = ggplot2::element_text(size = 21),
    legend.text         = ggplot2::element_text(size = 14)
  )

zusammenfassung <- jahresdaten_mit_vorzeichen |>
  dplyr::group_by(migration_sign) |>
  dplyr::summarize(
    Anzahl     = n(),
    Mittelwert = mean(netto_zuwanderung),
    Median     = median(netto_zuwanderung),
    SD         = sd(netto_zuwanderung),
    Min        = min(netto_zuwanderung),
    Max        = max(netto_zuwanderung),
    IQR        = IQR(netto_zuwanderung)
  )

laender_gesamt <- rohe_schweizer_einwanderung |>
  dplyr::group_by(herkunft) |>
  dplyr::summarise(
    gesamt_netto_zuwanderung = sum(netto_zuwanderung, na.rm = TRUE),
    .groups = "drop"
  ) |>
  dplyr::arrange(desc(gesamt_netto_zuwanderung)) |>
  dplyr::mutate(
    herkunft_de = ifelse(
      herkunft == "Kosovo", "Kosovo",
      countrycode::countrycode(
        herkunft, origin = "country.name.en", destination = "country.name.de"
      )
    )
  )

horizontales_balkendiagramm <- ggplot2::ggplot(
  laender_gesamt,
  ggplot2::aes(x = gesamt_netto_zuwanderung,
               y = reorder(herkunft_de, gesamt_netto_zuwanderung))
) +
  ggplot2::geom_col(fill = "#08519c", color = "white", linewidth = 0.2) +
  ggplot2::geom_text(
    ggplot2::aes(label = scales::comma(gesamt_netto_zuwanderung,
                                       big.mark = ".", decimal.mark = ",")),
    hjust = -0.1, color = "black", size = 4
  ) +
  ggplot2::labs(x = "Gesamt Nettozuwanderung", y = "Herkunftsland",
                title = "Gesamt Nettozuwanderung in die Schweiz nach Herkunftsland") +
  ggplot2::theme_minimal() +
  ggplot2::scale_x_continuous(
    labels = scales::comma_format(big.mark = ".", decimal.mark = ",")) +
  ggplot2::theme(
    plot.title  = ggplot2::element_text(face = "plain", size = 16, hjust = 0.5,
                                        margin = ggplot2::margin(b = 10)),
    axis.title  = ggplot2::element_text(size = 20),
    axis.text   = ggplot2::element_text(size = 20),
    axis.text.y = ggplot2::element_text(size = 17, margin = ggplot2::margin(r = 5))
  )
```

```{r}
#| label: regressionen

regressionsdaten <- analysedaten_schweiz |>
  dplyr::select(netto_zuwanderung, bip_wachstum, arbeitslosenquote)

readr::write_csv(regressionsdaten, "data/regressionsdaten.csv")

modell_1_bip <- lm(netto_zuwanderung ~ bip_wachstum, data = regressionsdaten)

modell_2_bip_arbeitslosenquote <- lm(
  netto_zuwanderung ~ bip_wachstum + arbeitslosenquote,
  data = regressionsdaten
)

korrelationsmatrix <- cor(regressionsdaten)

acf_objekt <- stats::acf(
  residuals(modell_2_bip_arbeitslosenquote),
  plot = FALSE, na.action = na.pass
)

n_beobachtungen <- length(stats::na.omit(
  residuals(modell_2_bip_arbeitslosenquote)
))
konfidenzgrenze <- 1.96 / sqrt(n_beobachtungen)

autokorrelations_daten <- tibble::tibble(
  lag = as.numeric(acf_objekt$lag),
  acf = as.numeric(acf_objekt$acf)
) |>
  dplyr::filter(lag > 0)
```

# Forschungsfrage und Hypothesen

Diese Arbeit untersucht den Effekt des BIP-Wachstums auf die Nettozuwanderung
in die Schweiz, kontrolliert für die Arbeitslosenquote. Es wird erwartet, dass
die Nettozuwanderung stark durch das BIP-Wachstum beeinflusst wird, da dies
ein direkter Indikator für die Wirtschaftslage ist und die Einwanderung direkt
beeinflusst.

Die Arbeitslosenquote wird als Kontrollvariable einbezogen, da sie als
Arbeitsmarktindikator die Einwanderung auf direktere Weise beeinflusst.
Durch den langen Beobachtungszeitraum wird die Analyse weniger durch einzelne
Ereignisse wie die Finanzkrise 2008 verzerrt.

Die zwei Regressionsmodelle lauten:

$$\text{Nettozuwanderung}_i = \beta_0 + \beta_1 \times \text{BIP-Wachstum}_i + \varepsilon_i$$

$$\text{Nettozuwanderung}_i = \beta_0 + \beta_1 \times \text{BIP-Wachstum}_i + \beta_2 \times \text{Arbeitslosenquote}_i + \varepsilon_i$$

Dabei repräsentiert $\beta_0$ die Konstante, $\beta_1$ den Effekt des
BIP-Wachstums, $\beta_2$ den Effekt der Arbeitslosenquote und $\varepsilon_i$
den Fehlerterm, der alle nicht erfassten Abweichungen enthält.

# Daten

## Datenquellen

Die Schweizer Einwanderungsdaten (abhängige Variable) stammen vom Bundesamt
für Statistik (BFS) und wurden als CSV-Dateien manuell heruntergeladen. Sie
enthalten Angaben zu Herkunftsland, Jahr und Nettozuwanderung für den Zeitraum
1991–2024. Der Datensatz umfasst 15 europäische Länder, davon die Mehrheit der
EU-Länder. Die Beschränkung auf 15 Länder geht auf einen bestehenden Datensatz
einer Masterarbeit zurück und könnte die Repräsentativität der Ergebnisse
begrenzen. Die folgende Tabelle zeigt alle enthaltenen Herkunftsländer:

```{r}
#| label: laender-tabelle

knitr::kable(
  data.frame(Herkunftsland = karten_laender_liste),
  align = "c"
) |>
  kableExtra::kable_styling(full_width = FALSE, position = "center")
```

Die wirtschaftlichen Indikatoren (unabhängige und Kontrollvariable) stammen
von der Weltbank:

- Jährliches BIP-Wachstum (Weltbank-ID: `NY.GDP.MKTP.KD.ZG`)
- Arbeitslosenquote (Weltbank-ID: `SL.UEM.TOTL.ZS`)

# Deskriptive Statistik

Die Nettozuwanderung weist ein Minimum von −10'589 auf, was zeigt, dass es
Jahre gab, in denen die Schweiz mehr Aus- als Einwanderungen verzeichnete.
Der Median von 29'210 liegt unter dem Mittelwert von 32'340, was auf einzelne
Jahre mit besonders hoher Zuwanderung hindeutet.

```{r}
#| label: plot-boxplot
#| fig-width: 9
#| fig-height: 5

boxplot_vorzeichen
```

```{r}
#| label: zusammenfassung-tabelle

knitr::kable(
  zusammenfassung,
  digits      = 0,
  format.args = list(big.mark = "'"),
  caption     = "Deskriptive Kennzahlen nach Migrationsrichtung."
)
```

Es gibt nur 4 Jahre mit negativer Nettozuwanderung gegenüber 30 Jahren mit
positiver Nettozuwanderung.

## Zeitliche Entwicklung der Nettozuwanderung

Die Nettozuwanderung in die Schweiz durchlief unterschiedliche Phasen: einen
Rückgang zwischen 1995 und 2000 (inklusive einer Auswanderungsphase), ein
starkes Wachstum ab 2000, einen Rückgang während der Finanzkrise 2008 mit
anschliessender Erholung sowie einen erneuten Rückgang nach 2014. Wie bereits
gezeigt, überwiegen die Einwanderungsjahre insgesamt deutlich.

```{r}
#| label: plot-zeitreihe
#| fig-width: 10
#| fig-height: 5

kurven_diagramm
```

## Nettozuwanderung nach Herkunftsland

Die geographische Nähe spielt eine wichtige Rolle. Deutschland und Frankreich,
die beiden wichtigsten Nachbarländer der Schweiz, verzeichnen mit Abstand die
meisten Einwanderungen. Überraschenderweise weist Portugal eine höhere
Einwanderung als Italien auf, was möglicherweise damit zusammenhängt, dass
viele italienische Arbeitnehmer als Grenzgänger in der Schweiz arbeiten, aber
ihren Wohnsitz in Italien behalten und daher nicht als Einwanderer gezählt werden.

Spanien zeigt sogar eine negative Nettozuwanderung — mehr Menschen wanderten
aus der Schweiz nach Spanien aus als umgekehrt. Eine mögliche Erklärung ist,
dass Spanier nach der Pensionierung in ihre Heimat zurückziehen.

```{r}
#| label: plot-balken
#| fig-width: 10
#| fig-height: 6

horizontales_balkendiagramm
```

```{r}
#| label: plot-karte
#| fig-width: 10
#| fig-height: 6

schweiz_migrations_weltkarte
```

Die kartografische Darstellung ergänzt das Balkendiagramm und macht die
geografische Konzentration auf einen Blick sichtbar: Deutschland dominiert
klar durch seine deutlich dunklere Einfärbung. Weiss dargestellte Länder
bedeuten dabei nicht, dass keine Migration stattgefunden hat — sie zeigen
lediglich, dass keine Daten für diese Länder im Datensatz vorhanden sind,
da die Analyse auf die 15 erfassten Herkunftsländer beschränkt ist.

## Deskriptive Statistik nach Migrationsrichtung

```{r}
#| label: deskriptiv-tabelle

deskriptive_tabelle <- analysedaten_schweiz |>
  dplyr::mutate(
    migrationsrichtung = ifelse(
      netto_zuwanderung >= 0, "Einwanderung", "Auswanderung"
    )
  ) |>
  dplyr::select(
    migrationsrichtung, netto_zuwanderung, bip_wachstum, arbeitslosenquote
  ) |>
  gtsummary::tbl_summary(
    by        = migrationsrichtung,
    label     = list(
      netto_zuwanderung ~ "Nettozuwanderung",
      bip_wachstum      ~ "BIP-Wachstum (%)",
      arbeitslosenquote ~ "Arbeitslosenquote (%)"
    ),
    statistic = list(all_continuous() ~ "{mean} ({sd})"),
    digits    = all_continuous() ~ 2
  ) |>
  gtsummary::modify_header(label = "**Variable**") |>
  gtsummary::bold_labels() |>
  gtsummary::modify_footnote(
    all_stat_cols() ~ "Mittelwert (Standardabweichung)"
  ) |>
  gtsummary::add_p() |>
  gtsummary::modify_footnote(
    p.value ~ "Wilcoxon-Rangsummentest"
  ) |>
  gtsummary::as_gt() |>
  gt::tab_header(
    title    = "Deskriptive Statistik nach Migrationsrichtung",
    subtitle = "Mittelwert (Standardabweichung)"
  )

deskriptive_tabelle
```

BIP-Wachstum (p = 0.8) und Arbeitslosenquote (p = 0.3) unterscheiden sich
zwischen Einwanderungs- und Auswanderungsjahren kaum — konsistent mit den
nicht signifikanten Regressionseffekten.

# Ergebnisse

## Regressionsanalyse

Es wurden zwei lineare Regressionsmodelle geschätzt, um die jährliche
Nettozuwanderung zu erklären:

- **Modell 1:** Nettozuwanderung ~ BIP-Wachstum
- **Modell 2:** Nettozuwanderung ~ BIP-Wachstum + Arbeitslosenquote

```{r}
#| label: regression-tabelle
#| results: asis

if (knitr::is_html_output()) {
  texreg::htmlreg(
    list(Modell_1 = modell_1_bip, Modell_2 = modell_2_bip_arbeitslosenquote),
    custom.model.names = c("Modell 1", "Modell 2"),
    custom.coef.names  = c("(Intercept)", "BIP-Wachstum", "Arbeitslosenquote"),
    custom.note        = "Abhängige Variable: Nettozuwanderung. Signifikanz: * p < 0.05, ** p < 0.01, *** p < 0.001",
    digits = 3, single.row = FALSE, include.f = TRUE, caption = ""
  )
} else {
  texreg::texreg(
    list(Modell_1 = modell_1_bip, Modell_2 = modell_2_bip_arbeitslosenquote),
    custom.model.names = c("Modell 1", "Modell 2"),
    custom.coef.names  = c("(Intercept)", "BIP-Wachstum", "Arbeitslosenquote"),
    custom.note        = "Abhängige Variable: Nettozuwanderung. Signifikanz: * p < 0.05, ** p < 0.01, *** p < 0.001",
    digits = 3, single.row = FALSE, include.f = TRUE,
    caption = "Regressionsergebnisse: Nettozuwanderung", label = "tab:regression"
  )
}
```

Modell 1 zeigt, dass ein Anstieg des BIP-Wachstums um einen Prozentpunkt mit
einer um durchschnittlich 1'163 Personen höheren Nettozuwanderung verbunden ist,
dieser Effekt ist jedoch statistisch nicht signifikant (p > 0.05). Das Modell
erklärt nur 0.9 % der Variation (R² = 0.009).

In Modell 2 ist unter Kontrolle der Arbeitslosenquote ein zusätzlicher
Prozentpunkt BIP-Wachstum mit 187 Personen weniger Nettozuwanderung verbunden
(nicht signifikant). Ein Anstieg der Arbeitslosenquote um einen Prozentpunkt
ist mit einer um durchschnittlich 11'709 Personen höheren Nettozuwanderung
verbunden (hoch signifikant, p < 0.01). R² = 0.214.

Die Veränderung des BIP-Koeffizienten zwischen den beiden Modellen deutet auf
einen Omitted-Variable-Bias hin. Da BIP-Wachstum und Arbeitslosigkeit positiv
korreliert sind (r = 0.238, vgl. Korrelationsmatrix) und die Arbeitslosenquote
einen starken positiven Einfluss auf die Nettozuwanderung hat, wird in Modell 1
ein Teil dieses Effekts dem BIP-Wachstum zugeschrieben. Durch die Kontrolle der
Arbeitslosenquote in Modell 2 wird der direkte Effekt des BIP-Wachstums
isoliert, wodurch sich der Koeffizient von +1'163 auf −187 verändert.
Der Korrelationswert von r = 0.238 zwischen den Prädiktoren spricht für eine
schwache Korrelation und weist auf kein Multikollinearitätsproblem hin:

```{r}
#| label: korrelations-tabelle

knitr::kable(
  round(korrelationsmatrix, 3)
) |>
  kableExtra::kable_styling(full_width = FALSE, position = "center")
```

# Diagnostik

## Autokorrelationsanalyse

```{r}
#| label: acf-tabelle

knitr::kable(
  head(autokorrelations_daten, 4),
  digits = 3,
  col.names = c("Lag", "Autokorrelation")
) |>
  kableExtra::kable_styling(full_width = FALSE, position = "center")
```

Die ersten vier Lags — insbesondere Lag 1 (r = 0.74) und Lag 2 (r = 0.38),
beide über dem 95%-Konfidenzintervall von ±`r round(konfidenzgrenze, 3)` —
zeigen eine starke zeitliche Abhängigkeit der Residuen. Die Fehlerterme
schwanken nicht zufällig, sondern folgen zeitlichen Mustern. Dies verletzt
eine zentrale Annahme der OLS-Regression (Unabhängigkeit der Fehlerterme)
und deutet auf folgende Probleme hin:

1. **Fehlender Zeittrend:** Die Nettozuwanderung könnte einem langfristigen
   Aufwärts- oder Abwärtstrend folgen, der im Modell nicht erfasst wird.
2. **Fehlende Lag-Variablen:** Die Nettozuwanderung im Jahr t hängt
   möglicherweise von jener im Jahr t−1 ab.
3. **Verbesserungsbedarf:** Die Aufnahme eines Zeittrends oder einer
   verzögerten abhängigen Variable würde das Modell verbessern.

Fazit: Das Modell erfasst eine zeitliche Komponente nicht vollständig.
Die hohe Autokorrelation deutet darauf hin, dass vergangene Migrationswerte
die künftigen besser vorhersagen als die wirtschaftlichen Variablen allein
es erlauben.

# Diskussion

Die Analyse zeigt mehrere wichtige Muster der Schweizer Migration. Erstens
weist die Nettozuwanderung eine hohe Volatilität auf, wobei wenige Jahre mit
extrem hoher Einwanderung den Gesamttrend dominieren. Die Finanzkrise 2008
hatte einen sichtbaren, aber vorübergehenden Einfluss.

Zweitens spielt die geographische Nähe eine entscheidende Rolle. Deutschland
und Frankreich verzeichnen mit Abstand die meisten Einwanderungen. Spaniens
negative Nettozuwanderung deutet auf Rückwanderungsmuster hin, möglicherweise
nach der Pensionierung.

Drittens zeigen wirtschaftliche Indikatoren allein eine begrenzte
Erklärungskraft. Die kontraintuitive positive Beziehung zwischen
Arbeitslosigkeit und Nettozuwanderung deutet auf einen Omitted-Variable-Bias hin.

Viertens bestehen wichtige methodische Einschränkungen: kleine Stichprobengrosse
(N = 34), Beschränkung auf 15 Herkunftsländer, starke Autokorrelation sowie
fehlende Variablen wie Migrationspolitik und wirtschaftliche Bedingungen in
den Herkunftsländern.

Die Analyse deutet insgesamt darauf hin, dass institutionelle und politische
Faktoren — insbesondere das Freizügigkeitsabkommen mit der EU (2002) — einen
erheblichen Einfluss haben, der von diesem Modell nicht erfasst wird.

# Schlussfolgerung

Obwohl inländische wirtschaftliche Indikatoren einen gewissen Zusammenhang
mit der Nettozuwanderung in die Schweiz zeigen, erklären sie nur einen kleinen
Teil der Variation. Die Analyse zeigt, dass die Schweizer Migrationsmuster
komplex sind und von mehreren Faktoren beeinflusst werden, die über einfache
wirtschaftliche Indikatoren hinausgehen.

Für zukünftige Forschung wird empfohlen:

- Institutionelle Faktoren wie bilaterale Abkommen und Politikänderungen einzubeziehen
- Wirtschaftliche und demografische Daten der Herkunftsländer zu integrieren
- Zeitreihenökonometrische Methoden anzuwenden, um die Autokorrelation zu behandeln
- Den Datensatz auf alle Herkunftsländer auszuweiten
